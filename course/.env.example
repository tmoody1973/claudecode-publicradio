# OpenRouter — powers the "Ask the course" AI chat.
# Get a free key at https://openrouter.ai/keys
# The models this site uses cost $0 (see lib/models.ts), but OpenRouter still
# requires a key to attribute the free-tier rate limit to an account.
#
# Without this key the site works completely — all 10 modules, 50 use cases,
# the glossary and the cost simulator. Only the chat degrades, to a friendly
# "not switched on yet" card.
OPENROUTER_API_KEY=
